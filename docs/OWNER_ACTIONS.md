# StockBox 2.0 Owner Actions

Required before public launch. These are external actions that cannot be truthfully completed from the local repository alone.

1. **Supabase production:** production schema is synchronized through `20260829135927_release_fk_indexes.sql`, including affiliate operations, referral clicks/custom ambassador quotas, payout clawbacks/hardening and atomic ambassador role/profile management. Supabase Auth leaked-password protection is unavailable on the current free plan, so it is tracked as optional future hardening rather than a launch blocker. Production signup/email-confirmation has been owner-verified; keep final browser auth recovery in device QA. Keep future migration versions synchronized between the repository and production.
2. **Stripe + Connect:** live Basic 2.0 pricing is confirmed at 79 SEK/month, the valid three-month launch coupon reduces it to 49 SEK/month, and the active `/api/stripe/webhook` endpoint subscribes to subscription, invoice-paid, refund and dispute events. Checkout/Portal was already owner-verified on the prior domain, and ambassador creation/login/dashboard is verified in Preview. After merge, run only a quick getstockbox.app billing redirect smoke, then complete one real ambassador Stripe Connect onboarding and verify referral commission/reversal/exactly-once payout before enabling affiliate payouts broadly.
3. **Financial providers:** production health currently reports SEC Companyfacts configured with an explicit user-agent and an explicit Yahoo → Stooq market-data chain. Remaining owner action is commercial/provider approval: confirm that the selected production data sources and intended StockBox usage comply with applicable provider terms/licensing. Do not promise estimates/news/transcripts or unsupported coverage until a separately licensed and tested source is configured.
4. **Vercel/domain:** the `stockbox-2-0` project, GitHub integration, both production domains and the release-candidate preview are live. The release Preview now has Supabase/Auth configuration, no longer shows the account-unavailable fallback, and anonymous Settings/Affiliate/Admin routes redirect to login. Remaining owner action: verify the production-only payout cron secret, complete browser/device flows after the final deploy, and merge the release PR/branch because the connected GitHub integration cannot create the PR (403 write permission).
5. **Legal/commercial facts:** supply/approve legal entity name, required address, support contact, privacy contact, governing law, VAT/refund policy, retention/deletion policy, Terms, Privacy and the final financial disclaimer. These facts must not be invented in code or documentation.
6. **Operations:** configure `ADMIN_EMAILS`; PostHog privacy boundaries are already hardened. Resend / Strong Buy delivery is optional and explicitly deferred until after launch, so it is not a release blocker.
7. **Final human QA:** automated accessibility regressions are present locally, but production still requires manual mobile/tablet/desktop QA for responsive layout, keyboard navigation, focus states, contrast, browser console, auth recovery, canonical company selection, watchlist/portfolio persistence, billing, admin denial for customers and production RLS isolation.

Current external blockers are now human/external validation rather than missing repository implementation: one real Stripe Connect ambassador onboarding plus payout/refund E2E, production-only Vercel secret review (especially the payout cron secret), provider licensing approval, legal/commercial approval, and final browser/device QA. Supabase CLI/Docker are still not installed locally, but the production migration chain itself has been applied and verified through `20260829135927_release_fk_indexes`.
